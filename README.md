# python-library-
기본적으로 설치할 수 있는 파이썬 라이브러리 

아나콘다 prompt에서 설치하면 됨. 복붙해서 붙여넣기 

``` text 
pip install openai
pip install langchain
pip install google-cloud-aiplatform
pip install google-genai
pip install bardapi
pip install transformers
pip install pinecone-client
pip install qdrant-client
pip install chromadb
pip install guidance
pip install gradio
pip install fastapi


# 데이터 과학 라이브러리
pip install numpy pandas matplotlib seaborn scikit-learn statsmodels

# 심화 기계학습 및 데이터 처리
pip install tensorflow  # 또는 pip install tensorflow-gpu (GPU 지원 버전)
pip install torch torchvision torchaudio # PyTorch
pip install dask ray

# 데이터베이스 및 데이터 수집
pip install SQLAlchemy
pip install beautifulsoup4 scrapy

# 고급 데이터 분석 및 전문 분야
pip install NetworkX
pip install nltk spaCy

# 웹 개발 라이브러리
pip install Flask Django
pip install Flask-RESTful # Flask RESTful API
pip install djangorestframework # Django REST framework

# 프론트엔드 통합 및 배포
# React/Vue.js는 npm 또는 yarn을 통해 설치 (별도 설치 필요)
pip install gunicorn

# 웹 크롤링 라이브러리
pip install requests beautifulsoup4 selenium Scrapy

# GUI 개발 라이브러리
pip install tkinter # Tkinter는 파이썬 기본 설치에 포함되어 있을 수 있습니다.
pip install PyQt5 # 또는 pip install PySide2 (PyQt와 유사한 라이브러리)
pip install Kivy

# 멀티미디어 처리 라이브러리
pip install Pillow opencv-python scikit-image MoviePy imageio librosa PyDub

# 업무 자동화 라이브러리
pip install openpyxl xlrd xlwt PyPDF2 ReportLab
pip install PyAutoGUI
pip install apscheduler

# 자연어 처리 라이브러리
pip install nltk spaCy scikit-learn gensim transformers
pip install tensorflow # 또는 pip install tensorflow-gpu (GPU 지원 버전)
pip install torch torchvision torchaudio # PyTorch

# spaCy 모델 (예시)
python -m spacy download en_core_web_sm # 영어 모델 다운로드

```
